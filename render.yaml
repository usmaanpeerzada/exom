services:
  - type: web
    name: exom
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
